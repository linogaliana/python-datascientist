:::: {.content-visible when-profile="fr"}

::: {.callout-tip}
## Exercice 2: créer un dépôt sur `Github`

* Créer un dépôt en suivant les consignes ci-dessous.
  + __Créer ce dépôt privé__, cela permettra
dans l'exercice 3 d'activer notre jeton. Vous pourrez le rendre public
après l'exercice 3, c'est comme vous le souhaitez.
  + __Créer ce dépôt avec un `README.md`__ en cliquant sur la case `Add a README file`
  + __Ajouter un `.gitignore`__ en sélectionnant le modèle `Python`

:::

::::

:::: {.content-visible when-profile="en"}

::: {.callout-tip}
## Exercise 2: create a repository on `Github`

* Create a repository following the instructions below.
  + __Create this repository as private__, this will allow us to
activate our token in exercise 3. You can make it public
after exercise 3, it is up to you.
  + __Create this repository with a `README.md`__ by ticking the `Add a README file` box
  + __Add a `.gitignore`__ by selecting the `Python` template

:::

::::
