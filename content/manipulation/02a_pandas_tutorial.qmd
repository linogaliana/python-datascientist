---
title: "Introduction à Pandas"
title-en: "Introduction to Pandas"
author: Lino Galiana
tags:
  - Pandas
  - Pollution
  - Ademe
  - Tutoriel
  - Manipulation
categories:
  - Tutoriel
  - Manipulation
aliases: ../pandas.html
description: |
  `Pandas` est l'élément central de l'écosystème `Python` pour la _data science_. 
  Le succès récent de `Python` dans l'analyse de données tient beaucoup à `Pandas` qui a permis d'importer la
  logique `SQL` dans le langage `Python`. `Pandas` embarque énormément de
  fonctionalités qui permettent d'avoir des chaînes de traitement efficaces pour
  traiter des données de volumétrie moyenne (jusqu'à quelques Gigas). Au-delà
  de cette volumétrie, il faudra se tourner vers d'autres solutions
  (`DuckDB`, `Dask`, `Polars`, `Spark`...).
description-en: |
  `Pandas` is the central element of the `Python` ecosystem for _data science_.
  The recent success of `Python` in data analysis owes a lot to `Pandas`, which made it possible to import
  `SQL` logic into the `Python` language. `Pandas` embeds a huge number of
  features that allow efficient processing pipelines for
  medium-sized data (up to a few gigabytes). Beyond
  this volume, other solutions will be needed
  (`DuckDB`, `Dask`, `Polars`, `Spark`...).
bibliography: ../../reference.bib
image: https://minio.lab.sspcloud.fr/lgaliana/generative-art/pythonds/panda_stretching.png
links:
- icon: journal-text
  name: Documentation Pandas
  url: https://pandas.pydata.org/docs/
format:
  html: default
  typst: default
---

::: {.content-visible when-profile="fr"}

La partie `Pandas` a évolué récemment. Vous pouvez retrouver les contenus liés à `Pandas` dans les chapitres suivants:

- [Introduction à `Pandas` et à la logique du _DataFrame_](/content/manipulation/02_pandas_intro.qmd) ;
- [Associer des données avec `Pandas` : les jointures](/content/manipulation/02_pandas_joins.qmd) ;
- [Manipuler des données avec `Pandas` : statistiques et restructuration](/content/manipulation/02_pandas_stats.qmd) ;
- [Pandas : limites et écosystèmes alternatifs](/content/manipulation/02_pandas_beyond.qmd) ;

:::

::: {.content-visible when-profile="en"}

The `Pandas` part has recently evolved. You can find the content related to `Pandas` in the following chapters:

- [Introduction to `Pandas` and the logic of the _DataFrame_](/content/manipulation/02_pandas_intro.qmd);
- [Combining data with `Pandas`: joins](/content/manipulation/02_pandas_joins.qmd);
- [Handling data with `Pandas`: statistics and reshaping](/content/manipulation/02_pandas_stats.qmd);
- [Pandas: limits and alternative ecosystems](/content/manipulation/02_pandas_beyond.qmd);

:::
